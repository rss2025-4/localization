# Given a laser scan message, gives the maximum distance ray and the angle that ray forms with the origin.

# NOTE: angles are measured around
# the positive Z axis (counterclockwise, if Z is up)
# with zero angle being forward along the x axis

float32 angle # in radians

float32 distance